/**
 * Choose a deterministic random number between 0 and 100
 */
export declare function randomBin(seed: string): number;
/**
 * Choose a deterministic variant.
 */
export declare function chooseVariant(trackingId: string, code: string, variants: string[]): string;
